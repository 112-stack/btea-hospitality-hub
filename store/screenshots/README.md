# Store screenshots

Screenshots in this directory are captured from the built application during browser QA. They show the actual mobile breakpoint inside a 390 × 844 device viewport; surrounding device and caption artwork is presentation-only.

Regenerate after significant UI changes and rerun `npm run store:audit`.
